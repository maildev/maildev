auto-install-peers=true
strict-peer-dependencies=false
# Embed the README content in the publish metadata sent to the registry.
# Older pnpm versions never set the readme field when publishing (all 2.x/3.x
# registry docs are missing it); harmless belt-and-braces on newer pnpm.
embed-readme=true
